name: 02 - Workflow Events

on:
  push:
  pull_request:
  schedule:
    # Runs daily at 06:15 UTC (example cron — adjust as needed)
    - cron: "15 6 * * *"
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo "${{ github.event_name }}"
